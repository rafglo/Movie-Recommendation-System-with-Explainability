# Artifact Manifest

## Recommended To Keep

These artifacts support the current app and final evaluation:

```text
models/neumf_ranking_model.pth
models/ranking_user_encoder.pkl
models/ranking_item_encoder.pkl
models/ranking_genre_cols.pkl
models/neumf_model_small.pth
models/user_encoder.pkl
models/item_encoder.pkl
models/genre_cols.pkl
reports/topk_evaluation_summary.csv
reports/hybrid_weight_tuning.csv
reports/neumf_ranking_training_history.csv
reports/final_model_summary.md
reports/evaluation_runs/leaderboard.csv
```

## Optional

These are useful for the project narrative but not required by the app:

```text
reports/midtermpresentation.ipynb
notebooks/eda.ipynb
notebooks/model_baseline.ipynb
notebooks/model_content.ipynb
notebooks/model_neural_cf.ipynb
```

## Generated Experiment History

The timestamped files in this folder are generated by `eval_topk`:

```text
reports/evaluation_runs/
```

Keep selected benchmark runs for the final report. Older exploratory runs can
be deleted if repository size becomes an issue.

## Large Or Legacy Artifacts

Check whether these are still needed before final submission:

```text
models/neumf_model_1M.pth
mlruns/
mlflow.db
```

They may be useful for experiment tracking, but they are not required by the
current Streamlit app.
